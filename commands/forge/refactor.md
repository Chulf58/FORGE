Run the FORGE refactor pipeline.

1. **Refactor agent:** writes refactor plan to `docs/context/handoff.md`
2. **Reviewer-triage → reviewers** (always include reviewer-style)
3. **Gate #2:** Ask for /forge:approve or /forge:discard.

After approval, run /forge:apply.

## What to refactor
$ARGUMENTS
