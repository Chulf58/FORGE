Run the FORGE debug pipeline.

1. **Debug agent:** traces root cause, writes fix to `docs/context/handoff.md`
2. **Reviewer-triage → reviewers**
3. **Gate #2:** Ask for /forge:approve or /forge:discard.

After approval, run /forge:apply.

## Bug description
$ARGUMENTS
