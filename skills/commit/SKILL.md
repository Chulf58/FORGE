---
name: forge:commit
description: "Deprecated — commit gates are now handled by /forge:approve. Use /forge:approve to both approve and execute commit+merge in one step."
allowed-tools: "Read"
---

This skill is deprecated. Commit gate approve+commit is handled entirely by
`/forge:approve` (Step 4).

If you have a pending commit gate, run `/forge:approve` to approve it AND
execute commit+merge in a single step.
